{
  "name": "directus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx directus start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-db": "cd ../db-files && bash ./install-database.sh postgres postgres 127.0.0.1 5432",
    "postinstall-db": "npx directus database migrate:latest",
    "dump-db": "cd ../db-files && bash ./dump-database.sh postgres postgres 127.0.0.1 5432"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "directus": "^11.17.4",
    "pg": "^8.22.0"
  }
}
